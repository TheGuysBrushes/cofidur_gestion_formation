{% extends 'AppCofidurBundle::Page/User/user.html.twig' %}

{% block body %}
    <h1>Affichage de tous les utilisateurs</h1>

<table>
	<tr>
        <th> MATRICULE </th>
		<th> NOM D'UTILISATEUR </th>
        <th> PRENOM </th>
        <th> NOM </th>
		{# <th> DATE DE NAISSANCE </th> #}
	</tr>
{% for user in users %}
	<tr>
		<td> {{ user.registrationNumber }} </td>
        <td> {{ user.username }} </td>
        <td> {{ user.firstName }} </td>
        <td> {{ user.lastName }} </td>
        {# <td> {{ user.dateOfBirth |date("m|d|Y") }} </td> #}
		<td> <a href="{{ path('AppCofidurBundle_user_edit', {'idOp': user.id}) }}">Editer</a>
		{#<td> <a href="{{ path('AppCofidurBundle_user_delete',   {'idOp': user.id}) }}">Supprimer</a>#}
	</tr>
{% endfor %}
</table>

<br/>

    <a href="{{ path('AppCofidurBundle_user_add') }}">Ajouter un opérateur</a>

{% endblock %}
